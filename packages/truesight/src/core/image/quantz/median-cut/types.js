// @flow

import type { ImageQuantizationParameters } from 'core/image/quantz/types';

// Used for configuring the median cut algorithm.
export type MedianCutParameters = ImageQuantizationParameters;
